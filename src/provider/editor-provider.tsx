"use client"

import {
  Dispatch,
  createContext,
  useContext,
  useEffect,
  useReducer,
} from "react";

const EditorProvider = () => {
  return <div>EditorProvider</div>;
};

export default EditorProvider;
